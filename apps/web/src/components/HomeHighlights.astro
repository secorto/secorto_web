---
import HighlightCard from './HighlightCard.astro'
import type { SectionType } from '@domain/section'
import type { UILanguages } from '@i18n/ui'

interface Props {
  highlights: { slug: string, section: SectionType }[]
  locale: UILanguages
}

const { highlights, locale } = Astro.props
---

<div class="highlights">
  <div class="highlights-cards">
    {highlights.map(({ slug, section }) => (
      <HighlightCard slug={slug} section={section} locale={locale} />
    ))}
  </div>
</div>
