---
import type { MarkdownLayoutProps } from 'astro';
import BaseLayout from '@layouts/BaseLayout.astro'
import LanguagePicker from '@components/LanguagePicker.astro'
import { buildStaticPageLinks } from '@i18n/languagePickerUtils'

type Props = MarkdownLayoutProps<{
  title: string
  canonicalSection?: string
}>;
const { frontmatter } = Astro.props;
const { title, canonicalSection } = frontmatter;

const links = buildStaticPageLinks(Astro.url, canonicalSection)
---

<BaseLayout pageTitle={title}>
  <LanguagePicker slot="langpicker" links={links} />
  <slot/>
</BaseLayout>
