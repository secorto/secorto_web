---
import BaseLayout from '@layouts/BaseLayout.astro'
import BlogPost from '@components/BlogPost.astro';
import { getCollection } from 'astro:content';
import BlogTags from '@components/BlogTags.astro';
const allPosts = await getCollection('talk');
const tags = [...new Set(allPosts.map((post) => post.data.tags).flat())];
const pageTitle = "Charla";
---
<BaseLayout pageTitle={pageTitle}>
  <BlogTags route="/charla/tags/" tags={tags} />
  <ul>
    {allPosts.map((post) => <BlogPost url={`/charla/${post.slug}`} title={post.data.title} />)}
  </ul>
</BaseLayout>
