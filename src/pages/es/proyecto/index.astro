---
import BaseLayout from '@layouts/BaseLayout.astro'
import ListExperience from '@components/ListExperience.astro';
import { getPostsByLocale } from '@utils/paths';
const allPosts = await getPostsByLocale('projects', 'es');
const pageTitle = "Proyectos";
---
<BaseLayout pageTitle={pageTitle}>
  <ListExperience posts={allPosts} basePath='es/proyecto' />
</BaseLayout>
